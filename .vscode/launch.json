{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "bun",
      "request": "attach",
      "name": "BunDebug",
      // This value can be retrieved by using `bun --inspect`.
      "url": "ws://localhost:6499/bun",
      "preLaunchTask": "bun-dev",
      "postDebugTask": "bun-dev-stop"
    }
  ]
}
